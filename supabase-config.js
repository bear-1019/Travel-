// TripBoard Supabase 設定檔
// 1. 到 Supabase 建立專案
// 2. 到 Project Settings → API 複製 Project URL 與 anon/public key
// 3. 貼到下面兩個欄位
// 注意：只能放 anon/public key，絕對不要放 service_role key。
window.TRIPBOARD_SUPABASE_URL = "";
window.TRIPBOARD_SUPABASE_ANON_KEY = "";
